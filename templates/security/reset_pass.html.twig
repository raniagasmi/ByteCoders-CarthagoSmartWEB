
{% block title %}Paswword Reset{% endblock %}

{% block body %}
<section class="container my-3">
    <div class="row">
        <div class="col">
            <h1>Pasword Reset</h1>
            {{ form_start(requestPassForm) }}
                {{ form_row(requestPassForm.addEmail) }}
                <button type="submit" class="btn btn-primary">Envoyer</button>
            {{ form_end(requestPassForm) }}
        </div>
    </div>
</section>

{% endblock %}
