<form method="post" action="{{ path('app_recyclagedechets_delete', {'idRecyc': recyclagedechet.idRecyc}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recyclagedechet.idRecyc) }}">
    <button class="btn">Delete</button>
</form>
