{% extends 'baseS.html.twig' %}

{% block title %}Typedechets{% endblock %}

{% block body %}
<div style="text-align: center; margin-top: 80px;">
    <h1>Typedechets</h1>

    <style>
        .custom-table {
            max-width: 50%; /* Définir une largeur maximale pour la table */
            margin: 0 auto; /* Centrer la table horizontalement */
        }
    </style>
{{ dump(typedechet) }}
<table class="table custom-table">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ typedechet.id }}</td>
        </tr>
        <tr>
            <th>Name</th>
            
            <td>{{ typedechet.name }}</td>
        </tr>
        <tr>
            <th>Categorie</th>
            <td>{{ typedechet.categorie }}</td>
        </tr>
        <tr>
            <td colspan="2" style="text-align: right;">
                
                <a href="{{ path('app_typedechets_index', {'id': typedechet.id}) }}" class="btn btn-info btn-sm">
                    <i class="bi bi-arrow-left"></i>
                </a>
               
                <a href="{{ path('app_typedechets_edit', {'id': typedechet.id}) }}" class="btn btn-info btn-sm">
                <i class="ri-pencil-line"></i> </a>
                

                {{ include('typedechets/_delete_form.html.twig') }}
                
            </td>
        </tr>
    </tbody>
</table>


    
{% endblock %}
