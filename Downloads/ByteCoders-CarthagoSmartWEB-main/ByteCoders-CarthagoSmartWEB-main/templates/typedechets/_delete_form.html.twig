<form method="post" action="{{ path('app_typedechets_delete', {'id': typedechet.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ typedechet.id) }}">
    <button class="btn">Delete</button>
</form>
