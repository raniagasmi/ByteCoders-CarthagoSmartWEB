<form method="post" action="{{ path('app_facture_delete', {'idFacture': facture.idFacture}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ facture.idFacture) }}">
    <button class="btn">Delete</button>
</form>
